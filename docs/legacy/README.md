# docs/legacy

Documents that are old, superseded, or wrong about the current build, kept as
a record. Each file opens with a banner saying what is wrong with it and where
the current truth lives. Nothing in here should be read as current design; the
live docs are one level up.
